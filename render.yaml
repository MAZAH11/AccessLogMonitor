services:
  # 1) Your React static site (free tier supported)
  - type: web
    runtime: static
    name: my-app-frontend
    repo: https://github.com/MAZAH11/LogMonitor
    branch: main
    rootDir: AccessLogMonitor/access-log-ui
    buildCommand: yarn install && yarn build
    staticPublishPath: build
    plan: starter

  # 2) Your C# backend (requires a paid plan)
  - type: web
    runtime: docker
    name: my-app-backend
    repo: https://github.com/MAZAH11/LogMonitor
    branch: main
    rootDir: AccessLogMonitor
    buildCommand: dotnet publish -c Release -o out
    dockerCommand: dotnet out/AccessLogMonitor.dll
    plan: starter
